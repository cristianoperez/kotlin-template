logentries:
  token: ~
logging:
  level:
    org:
      zalando:
        logbook: TRACE
management:
  endpoints:
    web:
      base-path: ~
spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/kotlin_spring_sample_test"
    username: postgres
    password: root
  flyway:
    # Turn turn on flyway migrattions
    enabled: true
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
